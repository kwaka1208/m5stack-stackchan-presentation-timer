# ESP-IDF build output
/build/

# Managed components are fetched automatically from idf_component.yml + dependencies.lock
/managed_components/

# sdkconfig backups (sdkconfig itself is committed)
sdkconfig.old
sdkconfig.ci

# Editor / OS
*.swp
*~
.DS_Store
